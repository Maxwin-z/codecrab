{
  "name": "@codeclaws/relay",
  "version": "0.1.0",
  "private": true,
  "description": "CodeClaws relay server — public network forwarding for LAN deployments",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@codeclaws/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
