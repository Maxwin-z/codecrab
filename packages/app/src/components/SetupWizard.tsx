// SetupWizard — first-run configuration flow (embedded in app)
//
// Steps:
//   1. Network mode selection (LAN / public relay)
//   2. Model configuration (scan local env, add API keys)
//   3. Browser capability setup (Chrome + DevTools MCP)
//   4. Plugin recommendations
//
// State persisted in Session Storage for resume on refresh
