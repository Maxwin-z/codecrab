// TanStack Router route tree
//
// Routes:
//   /            — ProjectList (select/create projects)
//   /chat        — ChatPage (main chat interface)
//   /setup       — SetupWizard (first-run configuration)
//   /settings    — Settings (model config, token management)
