// Model configuration API
//
// Endpoints:
//   GET    /api/models              — List all configured models
//   POST   /api/models              — Add a new model
//   PUT    /api/models/:id          — Update model config
//   DELETE /api/models/:id          — Remove a model
//   GET    /api/models/default      — Get global default model
//   PUT    /api/models/default      — Set global default model
//   GET    /api/models/project/:id  — Get project-specific model
//   PUT    /api/models/project/:id  — Set project-specific model
//
// Storage: ~/.codeclaws/models.json
